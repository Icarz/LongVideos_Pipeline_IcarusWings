# Long-form narrated-video pipeline — Python 3.12, Windows-first
anthropic            # script_gen + beat_plan (Claude)
openai               # image_gen (gpt-image-1)
edge-tts             # TTS voiceover
groq                 # forced alignment (Whisper) for word/segment timings
Pillow               # text cards, captions, watermark rendering
boto3                # Cloudflare R2 upload (S3-compatible) — stage 6, not yet built
google-api-python-client  # YouTube Data API — stage 6, not yet built
google-auth-oauthlib      # YouTube OAuth — stage 6, not yet built
python-dotenv        # .env loading
